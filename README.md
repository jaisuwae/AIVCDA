# AIVCDA
Project - AIVCDA ( Artificially Intelligent Voice Controlled Desktop Assistant ).  This project uses locally ran AI model to automate and do tasks just via the user's voice on a desktop environment.  Currently it only supports Windows but users can modify or rewrite the code to implement much more. thanks - jaisuwae.
